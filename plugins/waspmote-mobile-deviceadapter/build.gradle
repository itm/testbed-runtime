group = 'eu.smartsantander'
description = 'TR :: Plugins :: SmartSantanderMobileWaspmoteDeviceAdapter'

apply plugin: 'war'

dependencies {
    compile group: 'es.unican.tlmat.smartsantander', name: 'sms.waspmote-driver-mobile', version:'1.0-SNAPSHOT'
  providedCompile project(':tr.iwsn-common')
  providedCompile project(':tr.iwsn-gateway')
  providedCompile project(':tr.device-db')
    compile(group: 'org.osgi', name: 'org.osgi.core', version:'4.3.0') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
}
